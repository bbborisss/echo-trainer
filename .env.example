# Optional backend (see server/README.md). Leave unset to run fully offline:
# the game falls back to the rule-based coach and browser speech recognition.
# VITE_API_BASE=https://echo-chamber-api.<your-subdomain>.workers.dev
